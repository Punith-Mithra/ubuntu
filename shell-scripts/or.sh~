echo "please enter lowercase  char:\c"

read var

if [ `echo $var | wc -c` -eq 2 ]
then 

	if [ $var = a -o $var = e -o $var = i -o $var = o -o $var = u  ]
	then	
		echo "its a vowel "
	else
		echo "its not a vowel"
	fi
else
	echo "dont enter a string"
fi
